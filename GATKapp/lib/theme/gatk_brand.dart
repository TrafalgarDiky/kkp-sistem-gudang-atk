import 'package:flutter/material.dart';

/// Warna & konstanta visual GATK (selaras splash HTML).
abstract final class GatkBrand {
  static const bg = Color(0xFFF0F4FF);
  static const blue = Color(0xFF2356F6);
  static const textDark = Color(0xFF111827);
  static const muted = Color(0xFF9CA3AF);
  static const pillText = Color(0xFF374151);

  static const blob1 = Color(0xFFC7D6FF);
  static const blob2 = Color(0xFFFFD9A8);
  static const blob3 = Color(0xFFB8F0D8);

  /// Splash & login — latar gelap (slate-950).
  static const bgDark = Color(0xFF020617);
  /// Kartu / field di atas latar gelap.
  static const surfaceDark = Color(0xFF1E293B);
  static const borderDark = Color(0xFF475569);
  static const textOnDarkMuted = Color(0xFF94A3B8);
  static const textOnDark = Color(0xFFF1F5F9);

  /// Blob aksen di latar gelap (biru / amber / teal).
  static const blobDark1 = Color(0xFF2563EB);
  static const blobDark2 = Color(0xFFF59E0B);
  static const blobDark3 = Color(0xFF14B8A6);

  /// Logo badge baru: GA biru langit, TK putih, latar navy.
  static const logoNavy = Color(0xFF0F172A);
  static const logoSky = Color(0xFF0EA5E9);
  static const taglineMuted = Color(0xFF94A3B8);
}
